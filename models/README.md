# Local model artifacts

Place institution-approved Gemma checkpoints or runtime-specific model files in
this directory. Model binaries are intentionally ignored by Git and must not be
downloaded automatically by application startup.
