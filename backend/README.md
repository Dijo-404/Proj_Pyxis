# Pyxis Backend

FastAPI backend for the Member 1 Financial Intelligence & Risk Engine.

The current implementation keeps persistence in memory for hackathon development and isolates Gemma behind a provider interface so local runtimes can be wired in without changing the API flow.

